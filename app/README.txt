Installation guide:
Install Node.js, and NPM
Install Cake from coffescript website
In case you do not have the necessary Node.JS modules
open a console in the project directory and execute the command:
npm install
To compile the coffee and less files exdecue the command:
cake build
To start the server and application execute the commad:
node app.js
